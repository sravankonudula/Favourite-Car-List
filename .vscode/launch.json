{
    "configurations": [
        {
            "name": "Launch Program",
            "program": "${workspaceFolder}/server/config/app.js",
            "request": "launch",
            "skipFiles": [
                "<node_internals>/**"
            ],
            "type": "node"
        }
    ]
}